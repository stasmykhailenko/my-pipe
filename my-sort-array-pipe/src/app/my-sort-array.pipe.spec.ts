import { MySortArrayPipe } from './my-sort-array.pipe';

describe('MySortArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new MySortArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
